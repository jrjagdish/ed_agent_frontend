server {
    listen ${PORT};
    listen [::]:${PORT};

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    location /health {
        return 200 "ok";
        add_header Content-Type text/plain;
    }
}